{{ project_install_directory }}/log/traefik/*.json
{{ project_install_directory }}/log/traefik/*.log
{{ project_install_directory }}/log/cowire/*.json
{
        copytruncate
        create
	daily
        missingok
        notifempty
	rotate 30
	compress
        compresscmd /usr/bin/pigz
}